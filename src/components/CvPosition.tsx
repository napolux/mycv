import React from 'react';
import {
  Link
} from "react-router-dom";

export default class CvPosition extends React.Component {
  render() {
    return (
        <section>CvPosition</section>
    );
  }
}